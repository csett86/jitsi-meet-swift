// JitsiSignaling — Phase 1 implementation
// Covers: XMPP transport over WebSocket, XML stanza parsing,
// Multi-User Chat (MUC), Jingle session negotiation, and service discovery (disco#info / disco#items).

/// Top-level namespace for the JitsiSignaling library.
public enum JitsiSignaling {
    /// The version of the JitsiSignaling library.
    public static let version = "0.1.0"
}
